'use strict'

const sum = function (a, b) {
  return a + b
}
sum(1, 2)
const minus = function (a, b) {
  return a - b
}
minus(1, 2)
const multiply = function (a, b) {
  return a * b
}
multiply(1, 2)

const divide = function (a, b) {
  return a / b
}
divide(1, 2)
